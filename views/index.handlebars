<h1>Eat-Da-Burger!</h1>

<h2>Burgers that make you drool!</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            <p>{{this.id}}. {{this.burger_name}}
    {{!-- {{#> /partials/cat-block devoured=false}} --}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers you've already devoured!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
            <p>{{this.id}}. {{this.burger_name}}
    {{!-- {{#> /partials/cat-block devoured=true}} --}}
    {{/if}}
    {{/each}}
</ul>

<h2>Hungry for more?</h2>
<form class="create-form">

<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Already Eaten?</label><br>
    <input type="radio" name="sleepy" value="0"> Devoured<br>
    <input type="radio" name="sleepy" value="1"  checked> Ready to Eat
  </div>

  <button type="submit">Add Me Some Burger!</button>
</form>
